import React from 'react';
import ReactLoading from 'react-loading';

const LoadingPage = () => (
  <ReactLoading type="bubbles" color="lightpink" height={400} width={275} />
);

export default LoadingPage;
